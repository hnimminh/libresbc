- name: CODE DEPLOYEMENT LIBRESBC
  hosts: all
  gather_facts: true
  become: true

  vars:
    destinationdir: "{{versiondir}}/{{ version }}"
    firedaemon: restarted

  roles:
    - libre
    #- nginx
    #- freeswitch
    #- kamailio
    #- nftables
    #- captagent
