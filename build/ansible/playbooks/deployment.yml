- name: CODE DEPLOYEMENT LIBRESBC
  hosts: all
  gather_facts: true
  become: true

  vars:
    destinationdir: "{{libredir}}/{{version}}"

  roles:
    - role: platform
    - role: libre
      tags: [platform, libre]
    - role: nginx
      tags: [platform, application]
    - role: nftables
      tags: [platform, application]
    - role: captagent
      tags: [third-party]

