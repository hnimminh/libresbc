---
# liberator service
- name: deploy liberator
  import_tasks: "liberator.yml"
  tags: [libre, liberator]

# callng scripts
- name: deploy callng
  import_tasks: "callng.yml"
  tags: [libre, callng]
