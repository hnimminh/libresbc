- name: nginx installation
  import_tasks: nginx-install.yml
  tags: [third-party, nginx, nginx-install]

- name: nginx configuration
  import_tasks: nginx-config.yml
  tags: [third-party, nginx, nginx-config]
