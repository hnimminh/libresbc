---
# tasks file for calldp
#- name: Update configuration for callng
#  template:
#    src: "{{sourcedir}}/callng/configuration.lua"
#    dest: "{{destinationdir}}/callng/configuration.lua"
#    owner: root
#    group: root
#    mode: 0754

- name: Gather callng lua file
  find:
    paths: "{{destinationdir}}/callng"
    patterns: "*.lua"
    file_type: file
  register: fslua

- name: Update template for callng
  template:
    src: "{{sourcedir}}/callng/{{ item.path | basename }}"
    dest: "{{destinationdir}}/callng/{{ item.path | basename }}"
    owner: root
    group: root
    mode: '0754'
    #variable_start_string: '{{'
    #variable_end_string: '}}'
  with_items: "{{ fslua.files }}"
