        {%- for intconname, gateways in interconnections.items() %}
        <list name="{{intconname}}" total-weight="{{gateways.values()|sum}}">
          {%- for gateway, weight in gateways.items() %}
          <node name="{{gateway}}" weight="{{weight}}"/>
          {%- endfor %}
        </list>
        {%- endfor %}

